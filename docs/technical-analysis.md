# Sensor-detect 技术分析

## 目标

构建一个本地可运行的 AI 生成图像检测网站。第一阶段重点不是账号、部署、在线访问，而是打通上传图片、后端检测、前端展示结果的闭环。

## MVP 范围

- 单图上传检测
- React + TypeScript 前端页面
- 展示 AI / Real 概率
- 展示扫描动画、分析进度、频域、纹理、边缘、元数据等演示信号
- 展示检测模式、模型名称和风险提示
- 本地浏览器可访问
- 检测服务可替换，方便后续接入更强模型

## 技术选型

### 前端

当前版本使用 React、TypeScript、Vite 和 CSS 实现。页面视觉采用暖白主背景、钴蓝品牌色、深蓝黑检测面板，并使用细网格、扫描线、十字定位框、信号条等元素营造数字取证仪器感。

前端构建产物输出到 `frontend/dist`，由 Python 后端静态托管。开发时可在 `frontend/` 下执行 `npm run build` 重新生成产物。

### 后端

第一版使用 Python 标准库 HTTP 服务。原因是可以不依赖外部包先启动，避免本地环境、网络下载、深度学习依赖安装拖慢验证。

服务内部将检测逻辑封装为 `DetectorService`。未来切换 FastAPI、ONNX Runtime、PyTorch 或独立模型服务时，前端 API 不需要大改。

### 模型推理

默认模式：

- `demo`：本地无依赖也能运行，用图片基础特征生成结构化结果，仅用于链路验证。

可选真实模型模式：

- `model`：通过 Hugging Face `transformers` 的 `image-classification` pipeline 加载模型。
- 默认模型建议：`haywoodsloan/ai-image-detector-deploy`。

## 后续演进

### 第二阶段

- 改为 FastAPI
- 增加模型预热接口
- 支持批量检测
- 增加检测历史
- 增加图片大小、格式、尺寸校验

### 第三阶段

- 模型服务独立部署
- 增加 ONNX Runtime 推理
- 增加 GPU 支持
- 增加多模型投票
- 增加检测报告导出

### 生产阶段

- 用户系统
- 限流与审计
- 图片对象存储
- 数据库记录
- 异步任务队列
- 在线部署

## 风险

AI 生成图像检测不是确定性事实判断。模型在训练集外的新生成器、二次压缩、裁剪、重采样、截图转存等场景下可能失效。因此产品表达应使用“概率”“风险”“可信度”，避免使用“真图”“假图”的绝对措辞。